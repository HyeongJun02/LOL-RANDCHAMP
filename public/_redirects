# SPA 라우팅.
#
# /rooms/12 로 새로고침하면 서버가 그 경로의 파일을 찾는다. 그런 파일은
# 없으니 404가 뜬다. 라우팅은 브라우저에서 react-router가 하므로,
# 없는 경로는 전부 index.html을 200으로 돌려주고 거기서 이어받게 한다.
#
# 실제로 있는 파일(/sitemap.xml, /static/...)은 이 규칙보다 먼저 서빙되므로
# 여기 걸리지 않는다.
#
# Netlify는 vercel.json을 안 읽는다. 호스팅을 옮기면 이 파일도 같이 챙길 것.
/*    /index.html    200
